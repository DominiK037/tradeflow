spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tradeflow_ledger
    username: root
    password: local_dev_password
  jpa:
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans

logging:
1  level:
    io.tradeflow: DEBUG
     org.springframework.security: DEBUG
